<%= form_with(model: har_file, local: true) do |form| %>
  <% if har_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(har_file.errors.count, "error") %> prohibited this har_file from being saved:</h2>

      <ul>
      <% har_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :har_file_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :har_file_description %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.file_field :content, id: :har_file_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
